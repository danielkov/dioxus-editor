{
  "name": "editor-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "mise exec -- playwright test",
    "test:headed": "mise exec -- playwright test --headed",
    "test:debug": "mise exec -- playwright test --debug",
    "install-browsers": "mise exec -- playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
